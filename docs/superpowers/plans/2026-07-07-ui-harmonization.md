# UI Harmonization + Keyhole Icon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extend the config-driven dark theme to every window and the tray menu, and replace the app icon with a modern keyhole glyph.

**Architecture:** A `ThemePalette` class derives all colours from the existing `StyleConfig`; `Theme.Apply` injects palette brushes plus a `Theme.xaml` style dictionary into `Application.Resources` at startup and themes window backgrounds/title bars via a class handler. The tray menu gets a `ToolStripProfessionalRenderer` fed from the same palette. Icons are generated by a Pillow-only Python script mirroring the SVG geometry (no SVG rasterizer is installed on this machine).

**Tech Stack:** .NET 8 WPF + WinForms (net8.0-windows10.0.19041.0), xunit + Shouldly tests, Python 3.14 + Pillow for icon generation.

**Spec:** `docs/superpowers/specs/2026-07-07-ui-harmonization-design.md`

## Global Constraints

- No system dotnet: build/test with the local SDK, `~/.dotnet-local/dotnet` (PowerShell: `& "$env:USERPROFILE\.dotnet-local\dotnet"`)
- No new NuGet dependencies
- Match codebase style: tab indentation, block-style `namespace { }`, `internal` visibility unless wider access is needed
- Test project layout: `pass-winmenu-tests/<Folder>/<Name>Tests.cs`, xunit `[Fact]` + Shouldly `ShouldBe`
- Python for icons: `python` (C:\Python314\python.exe), only dependency `pillow`
- Nothing in this plan changes `SelectionWindow`, `SelectionLabel`, or any config class
- Commit after every task; commit messages end with the two trailer lines used in this repo (author tag + Claude-Session link)

---

### Task 1: ThemePalette (colour derivation from StyleConfig)

**Files:**
- Create: `pass-winmenu/src/Windows/Theming/ThemePalette.cs`
- Test: `pass-winmenu-tests/Windows/ThemePaletteTests.cs`

**Interfaces:**
- Consumes: `PassWinmenu.Configuration.StyleConfig` (existing), `Helpers.BrushFromColourString` (existing, only in tests)
- Produces: `public sealed class ThemePalette` with ctor `ThemePalette(StyleConfig style)` and properties `WindowBackground, Foreground, HintForeground, Accent, AccentForeground, AccentHover, Link, ControlBackground, ControlBorder, ControlHover, ControlPressed` (all `System.Windows.Media.Color`) and `bool IsDark`. Static helpers `Mix(Color, Color, double)` and `Luminance(Color)` (internal).

- [ ] **Step 1: Write the failing tests**

`pass-winmenu-tests/Windows/ThemePaletteTests.cs`:

```csharp
using System.Windows.Media;
using PassWinmenu.Configuration;
using PassWinmenu.Utilities;
using PassWinmenu.Windows.Theming;
using Shouldly;
using Xunit;

namespace PassWinmenuTests.Windows
{
	public class ThemePaletteTests
	{
		private static StyleConfig DarkStyle() => new StyleConfig
		{
			BackgroundColour = Helpers.BrushFromColourString("#FF202020"),
			BorderColour = Helpers.BrushFromColourString("#FF0078D4"),
		};

		private static StyleConfig LightStyle() => new StyleConfig
		{
			BackgroundColour = Helpers.BrushFromColourString("#FFF5F5F5"),
			BorderColour = Helpers.BrushFromColourString("#FF0078D4"),
		};

		[Fact]
		public void DarkBackground_IsDetectedAsDark()
		{
			new ThemePalette(DarkStyle()).IsDark.ShouldBeTrue();
		}

		[Fact]
		public void LightBackground_IsDetectedAsLight()
		{
			new ThemePalette(LightStyle()).IsDark.ShouldBeFalse();
		}

		[Fact]
		public void DarkTheme_ControlShadesAreLighterThanBackground()
		{
			var palette = new ThemePalette(DarkStyle());
			ThemePalette.Luminance(palette.ControlBackground)
				.ShouldBeGreaterThan(ThemePalette.Luminance(palette.WindowBackground));
			ThemePalette.Luminance(palette.ControlBorder)
				.ShouldBeGreaterThan(ThemePalette.Luminance(palette.ControlBackground));
		}

		[Fact]
		public void LightTheme_ControlShadesAreDarkerThanBackground()
		{
			var palette = new ThemePalette(LightStyle());
			ThemePalette.Luminance(palette.ControlBackground)
				.ShouldBeLessThan(ThemePalette.Luminance(palette.WindowBackground));
		}

		[Fact]
		public void DarkTheme_LinkIsLighterThanAccent()
		{
			var palette = new ThemePalette(DarkStyle());
			ThemePalette.Luminance(palette.Link)
				.ShouldBeGreaterThan(ThemePalette.Luminance(palette.Accent));
		}

		[Fact]
		public void NonSolidBrush_FallsBackToDefaultColour()
		{
			var style = DarkStyle();
			style.BorderColour = new LinearGradientBrush(Colors.Red, Colors.Blue, 0);
			var palette = new ThemePalette(style);
			palette.Accent.ShouldBe(Color.FromRgb(0x00, 0x78, 0xD4));
		}

		[Fact]
		public void Luminance_IsZeroForBlackAndOneForWhite()
		{
			ThemePalette.Luminance(Colors.Black).ShouldBe(0.0, 0.001);
			ThemePalette.Luminance(Colors.White).ShouldBe(1.0, 0.001);
		}
	}
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `~/.dotnet-local/dotnet test pass-winmenu-tests --filter ThemePaletteTests`
Expected: build FAILURE, `ThemePalette` does not exist

- [ ] **Step 3: Write the implementation**

`pass-winmenu/src/Windows/Theming/ThemePalette.cs`:

```csharp
using System.Windows.Media;
using PassWinmenu.Configuration;

namespace PassWinmenu.Windows.Theming
{
	/// <summary>
	/// Derives the colour set used by the shared window theme and the tray menu
	/// from the user's <see cref="StyleConfig"/>. Control shades are computed
	/// relative to the configured background so a custom light config still
	/// produces usable contrast.
	/// </summary>
	public sealed class ThemePalette
	{
		public Color WindowBackground { get; }
		public Color Foreground { get; }
		public Color HintForeground { get; }
		public Color Accent { get; }
		public Color AccentForeground { get; }
		public Color AccentHover { get; }
		public Color Link { get; }
		public Color ControlBackground { get; }
		public Color ControlBorder { get; }
		public Color ControlHover { get; }
		public Color ControlPressed { get; }
		public bool IsDark { get; }

		public ThemePalette(StyleConfig style)
		{
			WindowBackground = GetColour(style.BackgroundColour, Color.FromRgb(0x20, 0x20, 0x20));
			Foreground = GetColour(style.Options.TextColour, Color.FromRgb(0xDD, 0xDD, 0xDD));
			HintForeground = GetColour(style.SearchHint.TextColour, Color.FromRgb(0x99, 0x99, 0x99));
			Accent = GetColour(style.BorderColour, Color.FromRgb(0x00, 0x78, 0xD4));

			IsDark = Luminance(WindowBackground) < 0.5;
			var shadeTarget = IsDark ? Colors.White : Colors.Black;

			AccentForeground = Luminance(Accent) < 0.5 ? Colors.White : Color.FromRgb(0x1A, 0x1A, 0x1A);
			AccentHover = Mix(Accent, shadeTarget, 0.12);
			Link = IsDark ? Mix(Accent, Colors.White, 0.35) : Accent;
			ControlBackground = Mix(WindowBackground, shadeTarget, 0.06);
			ControlHover = Mix(WindowBackground, shadeTarget, 0.11);
			ControlPressed = Mix(WindowBackground, shadeTarget, 0.15);
			ControlBorder = Mix(WindowBackground, shadeTarget, 0.17);
		}

		internal static Color Mix(Color from, Color to, double amount) => Color.FromRgb(
			(byte)(from.R + (to.R - from.R) * amount),
			(byte)(from.G + (to.G - from.G) * amount),
			(byte)(from.B + (to.B - from.B) * amount));

		internal static double Luminance(Color colour) =>
			(0.2126 * colour.R + 0.7152 * colour.G + 0.0722 * colour.B) / 255.0;

		private static Color GetColour(Brush brush, Color fallback) =>
			brush is SolidColorBrush solid ? solid.Color : fallback;
	}
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `~/.dotnet-local/dotnet test pass-winmenu-tests --filter ThemePaletteTests`
Expected: 7 tests PASS

- [ ] **Step 5: Commit**

```bash
git add pass-winmenu/src/Windows/Theming/ThemePalette.cs pass-winmenu-tests/Windows/ThemePaletteTests.cs
git commit -m "Add ThemePalette deriving UI colours from StyleConfig"
```

---

### Task 2: Theme dictionary and application-wide styles

**Files:**
- Create: `pass-winmenu/src/Windows/Theming/Theme.xaml`
- Create: `pass-winmenu/src/Windows/Theming/Theme.cs`
- Modify: `pass-winmenu/src/Setup.cs` (InitialiseDesktop, after `EmbeddedResources.Load()`)

**Interfaces:**
- Consumes: `ThemePalette` from Task 1
- Produces: `internal static class Theme` with `static ThemePalette? Current { get; }` and `static void Apply(StyleConfig style)`. Brush resource keys used by later tasks: `ThemeWindowBackgroundBrush, ThemeForegroundBrush, ThemeHintForegroundBrush, ThemeAccentBrush, ThemeAccentForegroundBrush, ThemeAccentHoverBrush, ThemeLinkBrush, ThemeControlBackgroundBrush, ThemeControlBorderBrush, ThemeControlHoverBrush, ThemeControlPressedBrush`. Also a private `OnWindowLoaded` class handler where Task 3 adds the title bar call.

Implicit styles apply to controls used directly (Button, TextBox, ...). They cannot apply to `Window` subclasses (WPF looks up implicit styles by concrete type), so window background/foreground are set from a `Window.Loaded` class handler, skipping windows that already set those properties locally (SelectionWindow, PasswordDetailsWindow).

- [ ] **Step 1: Create Theme.xaml**

`pass-winmenu/src/Windows/Theming/Theme.xaml` (Build Action defaults to Page in this SDK-style WPF project; no csproj change needed):

```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- Buttons -->
	<Style TargetType="Button">
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="Background" Value="{DynamicResource ThemeControlBackgroundBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorderBrush}" />
		<Setter Property="Padding" Value="10,4" />
		<Setter Property="MinHeight" Value="26" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Button">
					<Border x:Name="Chrome"
					        Background="{TemplateBinding Background}"
					        BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="1" CornerRadius="3" SnapsToDevicePixels="True">
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
						                  Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ThemeControlHoverBrush}" />
						</Trigger>
						<Trigger Property="IsPressed" Value="True">
							<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ThemeControlPressedBrush}" />
						</Trigger>
						<Trigger Property="IsKeyboardFocused" Value="True">
							<Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
						</Trigger>
						<Trigger Property="IsDefault" Value="True">
							<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ThemeAccentBrush}" />
							<Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
							<Setter Property="Foreground" Value="{DynamicResource ThemeAccentForegroundBrush}" />
						</Trigger>
						<MultiTrigger>
							<MultiTrigger.Conditions>
								<Condition Property="IsDefault" Value="True" />
								<Condition Property="IsMouseOver" Value="True" />
							</MultiTrigger.Conditions>
							<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ThemeAccentHoverBrush}" />
						</MultiTrigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.55" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Toggle buttons (e.g. the reveal-password eye) -->
	<Style TargetType="ToggleButton">
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="Background" Value="{DynamicResource ThemeControlBackgroundBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorderBrush}" />
		<Setter Property="Padding" Value="6,3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ToggleButton">
					<Border x:Name="Chrome"
					        Background="{TemplateBinding Background}"
					        BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="{TemplateBinding BorderThickness}"
					        CornerRadius="3" SnapsToDevicePixels="True">
						<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
						                  Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ThemeControlHoverBrush}" />
						</Trigger>
						<Trigger Property="IsChecked" Value="True">
							<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ThemeAccentBrush}" />
							<Setter Property="Foreground" Value="{DynamicResource ThemeAccentForegroundBrush}" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.55" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Text inputs -->
	<Style TargetType="TextBox">
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="Background" Value="{DynamicResource ThemeControlBackgroundBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorderBrush}" />
		<Setter Property="CaretBrush" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="SelectionBrush" Value="{DynamicResource ThemeAccentBrush}" />
		<Setter Property="Padding" Value="4,3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TextBox">
					<Border x:Name="Chrome"
					        Background="{TemplateBinding Background}"
					        BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="1" CornerRadius="3" SnapsToDevicePixels="True">
						<ScrollViewer x:Name="PART_ContentHost" Focusable="False" Margin="{TemplateBinding Padding}" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsKeyboardFocusWithin" Value="True">
							<Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.55" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="PasswordBox">
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="Background" Value="{DynamicResource ThemeControlBackgroundBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorderBrush}" />
		<Setter Property="CaretBrush" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="SelectionBrush" Value="{DynamicResource ThemeAccentBrush}" />
		<Setter Property="Padding" Value="4,3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="PasswordBox">
					<Border x:Name="Chrome"
					        Background="{TemplateBinding Background}"
					        BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="1" CornerRadius="3" SnapsToDevicePixels="True">
						<ScrollViewer x:Name="PART_ContentHost" Focusable="False" Margin="{TemplateBinding Padding}" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsKeyboardFocusWithin" Value="True">
							<Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Labels -->
	<Style TargetType="Label">
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
	</Style>

	<!-- Check boxes -->
	<Style TargetType="CheckBox">
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="CheckBox">
					<StackPanel Orientation="Horizontal" Background="Transparent">
						<Border x:Name="Box" Width="16" Height="16" CornerRadius="3"
						        Background="{DynamicResource ThemeControlBackgroundBrush}"
						        BorderBrush="{DynamicResource ThemeControlBorderBrush}"
						        BorderThickness="1" VerticalAlignment="Center">
							<Path x:Name="Check" Data="M3,8 L6.5,11.5 L13,4.5"
							      Stroke="{DynamicResource ThemeAccentForegroundBrush}"
							      StrokeThickness="2" Visibility="Collapsed" />
						</Border>
						<ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
					</StackPanel>
					<ControlTemplate.Triggers>
						<Trigger Property="IsChecked" Value="True">
							<Setter TargetName="Box" Property="Background" Value="{DynamicResource ThemeAccentBrush}" />
							<Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
							<Setter TargetName="Check" Property="Visibility" Value="Visible" />
						</Trigger>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter Property="Opacity" Value="0.55" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Group boxes -->
	<Style TargetType="GroupBox">
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="Padding" Value="6" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="GroupBox">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition />
						</Grid.RowDefinitions>
						<ContentPresenter ContentSource="Header" Margin="4,0,0,4"
						                  TextElement.Foreground="{DynamicResource ThemeHintForegroundBrush}" />
						<Border Grid.Row="1" BorderBrush="{DynamicResource ThemeControlBorderBrush}"
						        BorderThickness="1" CornerRadius="4" Padding="{TemplateBinding Padding}">
							<ContentPresenter />
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Sliders (horizontal only; that is all this app uses) -->
	<Style TargetType="Slider">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="Slider">
					<Grid MinHeight="20" Background="Transparent">
						<Border Height="4" CornerRadius="2" VerticalAlignment="Center"
						        Background="{DynamicResource ThemeControlBorderBrush}" />
						<Track x:Name="PART_Track">
							<Track.DecreaseRepeatButton>
								<RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
									<RepeatButton.Template>
										<ControlTemplate TargetType="RepeatButton">
											<Grid Background="Transparent">
												<Border Height="4" CornerRadius="2" VerticalAlignment="Center"
												        Background="{DynamicResource ThemeAccentBrush}" />
											</Grid>
										</ControlTemplate>
									</RepeatButton.Template>
								</RepeatButton>
							</Track.DecreaseRepeatButton>
							<Track.IncreaseRepeatButton>
								<RepeatButton Command="Slider.IncreaseLarge" Focusable="False">
									<RepeatButton.Template>
										<ControlTemplate TargetType="RepeatButton">
											<Grid Background="Transparent" />
										</ControlTemplate>
									</RepeatButton.Template>
								</RepeatButton>
							</Track.IncreaseRepeatButton>
							<Track.Thumb>
								<Thumb Width="14" Height="14" Focusable="False">
									<Thumb.Template>
										<ControlTemplate TargetType="Thumb">
											<Ellipse Fill="{DynamicResource ThemeAccentBrush}" />
										</ControlTemplate>
									</Thumb.Template>
								</Thumb>
							</Track.Thumb>
						</Track>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- Scroll bars -->
	<Style TargetType="ScrollBar">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="Width" Value="10" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ScrollBar">
					<Grid Background="Transparent">
						<Track x:Name="PART_Track" IsDirectionReversed="True">
							<Track.DecreaseRepeatButton>
								<RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False">
									<RepeatButton.Template>
										<ControlTemplate TargetType="RepeatButton">
											<Grid Background="Transparent" />
										</ControlTemplate>
									</RepeatButton.Template>
								</RepeatButton>
							</Track.DecreaseRepeatButton>
							<Track.IncreaseRepeatButton>
								<RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False">
									<RepeatButton.Template>
										<ControlTemplate TargetType="RepeatButton">
											<Grid Background="Transparent" />
										</ControlTemplate>
									</RepeatButton.Template>
								</RepeatButton>
							</Track.IncreaseRepeatButton>
							<Track.Thumb>
								<Thumb Focusable="False">
									<Thumb.Template>
										<ControlTemplate TargetType="Thumb">
											<Border Background="{DynamicResource ThemeControlBorderBrush}"
											        CornerRadius="4" Margin="2,0" />
										</ControlTemplate>
									</Thumb.Template>
								</Thumb>
							</Track.Thumb>
						</Track>
					</Grid>
					<ControlTemplate.Triggers>
						<Trigger Property="Orientation" Value="Horizontal">
							<Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Style.Triggers>
			<Trigger Property="Orientation" Value="Horizontal">
				<Setter Property="Width" Value="Auto" />
				<Setter Property="Height" Value="10" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<!-- Hyperlinks -->
	<Style TargetType="Hyperlink">
		<Setter Property="Foreground" Value="{DynamicResource ThemeLinkBrush}" />
		<Setter Property="TextDecorations" Value="{x:Null}" />
		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="TextDecorations" Value="Underline" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<!-- Tool tips -->
	<Style TargetType="ToolTip">
		<Setter Property="Background" Value="{DynamicResource ThemeControlBackgroundBrush}" />
		<Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}" />
		<Setter Property="BorderBrush" Value="{DynamicResource ThemeControlBorderBrush}" />
	</Style>

</ResourceDictionary>
```

- [ ] **Step 2: Create Theme.cs**

`pass-winmenu/src/Windows/Theming/Theme.cs`:

```csharp
using System;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Media;
using PassWinmenu.Configuration;

namespace PassWinmenu.Windows.Theming
{
	/// <summary>
	/// Applies the shared, config-driven theme to the whole application:
	/// palette brushes + implicit control styles in the application resources,
	/// and window background/foreground via a class handler (implicit styles
	/// cannot target Window subclasses).
	/// </summary>
	internal static class Theme
	{
		public static ThemePalette? Current { get; private set; }

		private static bool handlerRegistered;

		public static void Apply(StyleConfig style)
		{
			var palette = new ThemePalette(style);
			Current = palette;

			var app = Application.Current;
			if (app == null)
			{
				return;
			}

			app.Resources["ThemeWindowBackgroundBrush"] = Freeze(palette.WindowBackground);
			app.Resources["ThemeForegroundBrush"] = Freeze(palette.Foreground);
			app.Resources["ThemeHintForegroundBrush"] = Freeze(palette.HintForeground);
			app.Resources["ThemeAccentBrush"] = Freeze(palette.Accent);
			app.Resources["ThemeAccentForegroundBrush"] = Freeze(palette.AccentForeground);
			app.Resources["ThemeAccentHoverBrush"] = Freeze(palette.AccentHover);
			app.Resources["ThemeLinkBrush"] = Freeze(palette.Link);
			app.Resources["ThemeControlBackgroundBrush"] = Freeze(palette.ControlBackground);
			app.Resources["ThemeControlBorderBrush"] = Freeze(palette.ControlBorder);
			app.Resources["ThemeControlHoverBrush"] = Freeze(palette.ControlHover);
			app.Resources["ThemeControlPressedBrush"] = Freeze(palette.ControlPressed);

			app.Resources.MergedDictionaries.Add(new ResourceDictionary
			{
				Source = new Uri("pack://application:,,,/src/Windows/Theming/Theme.xaml"),
			});

			if (!handlerRegistered)
			{
				handlerRegistered = true;
				EventManager.RegisterClassHandler(typeof(Window), FrameworkElement.LoadedEvent,
					new RoutedEventHandler(OnWindowLoaded));
			}
		}

		private static void OnWindowLoaded(object sender, RoutedEventArgs e)
		{
			if (sender is not Window window)
			{
				return;
			}
			// Windows that style themselves (SelectionWindow, PasswordDetailsWindow)
			// set these locally; leave them alone.
			if (window.ReadLocalValue(Control.BackgroundProperty) == DependencyProperty.UnsetValue)
			{
				window.SetResourceReference(Control.BackgroundProperty, "ThemeWindowBackgroundBrush");
			}
			if (window.ReadLocalValue(Control.ForegroundProperty) == DependencyProperty.UnsetValue)
			{
				window.SetResourceReference(Control.ForegroundProperty, "ThemeForegroundBrush");
			}
		}

		private static SolidColorBrush Freeze(Color colour)
		{
			var brush = new SolidColorBrush(colour);
			brush.Freeze();
			return brush;
		}
	}
}
```

- [ ] **Step 3: Hook into startup**

In `pass-winmenu/src/Setup.cs`, method `InitialiseDesktop`, directly after `EmbeddedResources.Load();` add:

```csharp
			Theme.Apply(configManager.ConfigurationFile.Config.Interface.Style);
```

and add the using at the top: `using PassWinmenu.Windows.Theming;`

`InitialiseCommandLine` stays untouched (no WPF Application there).

- [ ] **Step 4: Build and smoke-test**

Run: `~/.dotnet-local/dotnet build pass-winmenu.sln`
Expected: build succeeds

Run the app (`pass-winmenu/bin/Debug/net8.0-windows10.0.19041.0/pass-winmenu.exe`), open the tray menu, open "About": window content area must be dark with themed button. Close the app.

- [ ] **Step 5: Run the full test suite**

Run: `~/.dotnet-local/dotnet test pass-winmenu.sln`
Expected: all tests PASS

- [ ] **Step 6: Commit**

```bash
git add pass-winmenu/src/Windows/Theming/ pass-winmenu/src/Setup.cs
git commit -m "Apply config-driven theme to all windows via shared resources"
```

---

### Task 3: Dark native title bars

**Files:**
- Create: `pass-winmenu/src/WinApi/DarkTitleBar.cs`
- Modify: `pass-winmenu/src/Windows/Theming/Theme.cs` (OnWindowLoaded)

**Interfaces:**
- Consumes: `Theme.Current` (Task 2), `Window.Loaded` class handler in `Theme.OnWindowLoaded`
- Produces: `internal static class DarkTitleBar` with `static void Apply(Window window)`

- [ ] **Step 1: Create DarkTitleBar.cs**

```csharp
using System;
using System.Runtime.InteropServices;
using System.Windows;
using System.Windows.Interop;

namespace PassWinmenu.WinApi
{
	/// <summary>
	/// Switches a window's non-client area (title bar) to the dark variant.
	/// Best-effort: on OS builds that do not support the attribute this is a no-op.
	/// </summary>
	internal static class DarkTitleBar
	{
		private const int DwmwaUseImmersiveDarkMode = 20;

		public static void Apply(Window window)
		{
			var handle = new WindowInteropHelper(window).Handle;
			if (handle == IntPtr.Zero)
			{
				return;
			}
			var enabled = 1;
			_ = DwmSetWindowAttribute(handle, DwmwaUseImmersiveDarkMode, ref enabled, sizeof(int));
		}

		[DllImport("dwmapi.dll", PreserveSig = true)]
		private static extern int DwmSetWindowAttribute(IntPtr hwnd, int attribute, ref int value, int size);
	}
}
```

- [ ] **Step 2: Call it from the theme's window handler**

In `Theme.OnWindowLoaded`, after the two `SetResourceReference` blocks, add:

```csharp
			if (Current is { IsDark: true })
			{
				WinApi.DarkTitleBar.Apply(window);
			}
```

- [ ] **Step 3: Build and verify**

Run: `~/.dotnet-local/dotnet build pass-winmenu.sln`
Expected: build succeeds

Run the app, open "About" and "Windows Hello setup" (tray > More Actions > Set up Windows Hello unlock, cancel it after looking): title bars must be dark.

- [ ] **Step 4: Commit**

```bash
git add pass-winmenu/src/WinApi/DarkTitleBar.cs pass-winmenu/src/Windows/Theming/Theme.cs
git commit -m "Render dark native title bars when the theme background is dark"
```

---

### Task 4: Window XAML cleanups (About refresh, hardcoded colours)

**Files:**
- Modify: `pass-winmenu/src/Windows/AboutWindow.xaml`
- Modify: `pass-winmenu/src/Windows/EditWindow.xaml:25` (PasswordDivider)

**Interfaces:**
- Consumes: brush keys from Task 2 (`ThemeHintForegroundBrush`, `ThemeForegroundBrush`, `ThemeAccentBrush`)
- Produces: nothing consumed later

Note: `PasswordDetailsWindow` is intentionally untouched. Its `ApplyStyle` already draws every colour from the same `StyleConfig`, so it is harmonized by construction; the theme's window handler skips it because it sets `Background` locally.

- [ ] **Step 1: Rewrite AboutWindow.xaml content**

Replace the full `<Grid>` content of `AboutWindow.xaml` with (window attributes: change `Height="270"` to `Height="290"`):

```xml
	<Grid Margin="20">
		<StackPanel>
			<StackPanel Orientation="Horizontal" Margin="0,0,0,12">
				<Path Data="M16,3 A13,13 0 1 0 16,29 A13,13 0 1 0 16,3 Z M16,7.3 A4.2,4.2 0 0 0 13.4,14.8 L11.2,23.5 L20.8,23.5 L18.6,14.8 A4.2,4.2 0 0 0 16,7.3 Z"
				      Fill="{DynamicResource ThemeForegroundBrush}"
				      Width="38" Height="38" Stretch="Uniform" Margin="0,2,12,0" />
				<StackPanel VerticalAlignment="Center">
					<TextBlock Text="Pass Winmenu 2" FontSize="20" FontWeight="Bold" />
					<TextBlock x:Name="VersionLine" Margin="0,2,0,0" Foreground="{DynamicResource ThemeHintForegroundBrush}" />
				</StackPanel>
			</StackPanel>
			<TextBlock TextWrapping="Wrap" Margin="0,0,0,10">
				A keyboard-friendly Windows interface for the
				<Hyperlink NavigateUri="https://www.passwordstore.org" RequestNavigate="Hyperlink_RequestNavigate">pass</Hyperlink>
				password manager, with fingerprint (Windows Hello) unlock.
			</TextBlock>
			<TextBlock Margin="0,0,0,2">Created by giovi321</TextBlock>
			<TextBlock Margin="0,0,0,12">
				<Hyperlink NavigateUri="https://github.com/giovi321/pass-winmenu-2" RequestNavigate="Hyperlink_RequestNavigate">github.com/giovi321/pass-winmenu-2</Hyperlink>
			</TextBlock>
			<TextBlock TextWrapping="Wrap" Foreground="{DynamicResource ThemeHintForegroundBrush}" Margin="0,0,0,16">
				Based on
				<Hyperlink NavigateUri="https://github.com/geluk/pass-winmenu" RequestNavigate="Hyperlink_RequestNavigate">pass-winmenu</Hyperlink>
				by Johan Geluk.
			</TextBlock>
			<Button x:Name="Btn_Close" Content="Close" Width="80" HorizontalAlignment="Right"
			        IsDefault="True" IsCancel="True" Click="Btn_Close_Click" />
		</StackPanel>
	</Grid>
```

(The `Path` data is the keyhole icon geometry; WPF's default fill rule is EvenOdd, which punches out the keyhole.)

- [ ] **Step 2: Fix the EditWindow divider colour**

In `EditWindow.xaml` line 25, change `Stroke="#569de5"` to `Stroke="{DynamicResource ThemeAccentBrush}"` on the `PasswordDivider` rectangle.

- [ ] **Step 3: Build and verify**

Run: `~/.dotnet-local/dotnet build pass-winmenu.sln`
Expected: build succeeds

Run the app and open About (icon + dark theme + accent links), Add new Password, and Edit Password File on a test entry: all controls (text boxes, buttons, slider, checkbox, group box) must render dark with accent highlights, no white patches except the text content areas being dark grey.

- [ ] **Step 4: Commit**

```bash
git add pass-winmenu/src/Windows/AboutWindow.xaml pass-winmenu/src/Windows/EditWindow.xaml
git commit -m "Refresh About window with keyhole mark, drop hardcoded dialog colours"
```

---

### Task 5: Themed tray menu

**Files:**
- Create: `pass-winmenu/src/Notifications/ThemedMenuColours.cs`
- Create: `pass-winmenu/src/Notifications/ThemedMenuRenderer.cs`
- Modify: `pass-winmenu/src/Notifications/Notifications.cs`
- Test: `pass-winmenu-tests/Notifications/ThemedMenuColoursTests.cs`

**Interfaces:**
- Consumes: `ThemePalette` (Task 1), `Theme.Current` (Task 2)
- Produces: `internal sealed record ThemedMenuColours(Color Background, Color Text, Color Hint, Color Link, Color SelectionBackground, Color SelectionText, Color Separator, Color Border)` (System.Drawing.Color) with `static ThemedMenuColours FromPalette(ThemePalette palette)`; `internal sealed class ThemedMenuRenderer : ToolStripProfessionalRenderer` with ctor `ThemedMenuRenderer(ThemedMenuColours colours)`

- [ ] **Step 1: Write the failing test**

`pass-winmenu-tests/Notifications/ThemedMenuColoursTests.cs`:

```csharp
using PassWinmenu.Configuration;
using PassWinmenu.Notifications;
using PassWinmenu.Utilities;
using PassWinmenu.Windows.Theming;
using Shouldly;
using Xunit;

namespace PassWinmenuTests.Notifications
{
	public class ThemedMenuColoursTests
	{
		[Fact]
		public void FromPalette_ConvertsWpfColoursToDrawingColours()
		{
			var style = new StyleConfig
			{
				BackgroundColour = Helpers.BrushFromColourString("#FF202020"),
				BorderColour = Helpers.BrushFromColourString("#FF0078D4"),
			};
			var palette = new ThemePalette(style);

			var colours = ThemedMenuColours.FromPalette(palette);

			colours.Text.R.ShouldBe(palette.Foreground.R);
			colours.Text.G.ShouldBe(palette.Foreground.G);
			colours.Text.B.ShouldBe(palette.Foreground.B);
			colours.SelectionBackground.R.ShouldBe(palette.Accent.R);
			colours.Background.ShouldNotBe(colours.SelectionBackground);
		}
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `~/.dotnet-local/dotnet test pass-winmenu-tests --filter ThemedMenuColoursTests`
Expected: build FAILURE, `ThemedMenuColours` does not exist

- [ ] **Step 3: Create ThemedMenuColours.cs**

```csharp
using System.Drawing;
using PassWinmenu.Windows.Theming;

namespace PassWinmenu.Notifications
{
	/// <summary>
	/// The WPF theme palette translated to System.Drawing colours for the
	/// WinForms tray menu.
	/// </summary>
	internal sealed record ThemedMenuColours(
		Color Background,
		Color Text,
		Color Hint,
		Color Link,
		Color SelectionBackground,
		Color SelectionText,
		Color Separator,
		Color Border)
	{
		public static ThemedMenuColours FromPalette(ThemePalette palette) => new(
			Background: ToDrawing(palette.ControlBackground),
			Text: ToDrawing(palette.Foreground),
			Hint: ToDrawing(palette.HintForeground),
			Link: ToDrawing(palette.Link),
			SelectionBackground: ToDrawing(palette.Accent),
			SelectionText: ToDrawing(palette.AccentForeground),
			Separator: ToDrawing(palette.ControlBorder),
			Border: ToDrawing(palette.ControlBorder));

		private static Color ToDrawing(System.Windows.Media.Color colour) =>
			Color.FromArgb(colour.A, colour.R, colour.G, colour.B);
	}
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `~/.dotnet-local/dotnet test pass-winmenu-tests --filter ThemedMenuColoursTests`
Expected: PASS

- [ ] **Step 5: Create ThemedMenuRenderer.cs**

```csharp
using System.Drawing;
using System.Drawing.Drawing2D;
using System.Windows.Forms;

namespace PassWinmenu.Notifications
{
	/// <summary>
	/// Renders the tray context menu with colours from the shared theme palette,
	/// so it matches the WPF windows.
	/// </summary>
	internal sealed class ThemedMenuRenderer : ToolStripProfessionalRenderer
	{
		private readonly ThemedMenuColours colours;

		public ThemedMenuRenderer(ThemedMenuColours colours)
			: base(new ThemedColourTable(colours))
		{
			this.colours = colours;
		}

		protected override void OnRenderItemText(ToolStripItemTextRenderEventArgs e)
		{
			if (e.Item.Enabled && e.Item.Selected)
			{
				e.TextColor = colours.SelectionText;
			}
			else if (e.Item.ForeColor.ToArgb() == SystemColors.ControlText.ToArgb())
			{
				// Item colour was never customised; use the themed default.
				e.TextColor = colours.Text;
			}
			base.OnRenderItemText(e);
		}

		protected override void OnRenderArrow(ToolStripArrowRenderEventArgs e)
		{
			e.ArrowColor = colours.Text;
			base.OnRenderArrow(e);
		}

		protected override void OnRenderItemCheck(ToolStripItemImageRenderEventArgs e)
		{
			// The stock check glyph is a dark bitmap; draw a themed check mark instead.
			var r = e.ImageRectangle;
			using var pen = new Pen(colours.Text, 1.8f);
			e.Graphics.SmoothingMode = SmoothingMode.AntiAlias;
			e.Graphics.DrawLines(pen, new[]
			{
				new PointF(r.Left + r.Width * 0.25f, r.Top + r.Height * 0.55f),
				new PointF(r.Left + r.Width * 0.45f, r.Top + r.Height * 0.75f),
				new PointF(r.Left + r.Width * 0.78f, r.Top + r.Height * 0.30f),
			});
		}

		private sealed class ThemedColourTable : ProfessionalColorTable
		{
			private readonly ThemedMenuColours colours;

			public ThemedColourTable(ThemedMenuColours colours)
			{
				this.colours = colours;
			}

			public override Color ToolStripDropDownBackground => colours.Background;
			public override Color ImageMarginGradientBegin => colours.Background;
			public override Color ImageMarginGradientMiddle => colours.Background;
			public override Color ImageMarginGradientEnd => colours.Background;
			public override Color MenuItemSelected => colours.SelectionBackground;
			public override Color MenuItemSelectedGradientBegin => colours.SelectionBackground;
			public override Color MenuItemSelectedGradientEnd => colours.SelectionBackground;
			public override Color MenuItemBorder => colours.SelectionBackground;
			public override Color MenuBorder => colours.Border;
			public override Color SeparatorDark => colours.Separator;
			public override Color SeparatorLight => colours.Background;
			public override Color CheckBackground => colours.Background;
			public override Color CheckSelectedBackground => colours.SelectionBackground;
			public override Color CheckPressedBackground => colours.SelectionBackground;
		}
	}
}
```

- [ ] **Step 6: Wire it up in Notifications.cs**

Changes in `pass-winmenu/src/Notifications/Notifications.cs`:

1. Add `using PassWinmenu.Windows.Theming;` to the usings.
2. In the constructor, replace the `downloadUpdate` initializer (remove the Beige highlight):

```csharp
			downloadUpdate = new ToolStripMenuItem("Download Update")
			{
				Visible = false,
			};
```

3. In `AddMenuActions`, replace the first three statements of the method body (the `new ContextMenuStrip()`, the `ToolStripLabel` add, and the `ToolStripSeparator` add) with:

```csharp
			var menu = new ContextMenuStrip();
			var colours = Theme.Current is { IsDark: true } palette ? ThemedMenuColours.FromPalette(palette) : null;
			if (colours != null)
			{
				menu.Renderer = new ThemedMenuRenderer(colours);
				menu.BackColor = colours.Background;
				menu.ForeColor = colours.Text;
				downloadUpdate.ForeColor = colours.Link;
			}
			var versionLabel = new ToolStripLabel("Pass Winmenu 2 " + Program.Version);
			if (colours != null)
			{
				versionLabel.ForeColor = colours.Hint;
			}
			menu.Items.Add(versionLabel);
			menu.Items.Add(new ToolStripSeparator());
```

4. After `var dropDown = new ToolStripMenuItem("More Actions");` add:

```csharp
			if (colours != null)
			{
				dropDown.DropDown.Renderer = new ThemedMenuRenderer(colours);
				dropDown.DropDown.BackColor = colours.Background;
				dropDown.DropDown.ForeColor = colours.Text;
			}
```

- [ ] **Step 7: Build and verify**

Run: `~/.dotnet-local/dotnet build pass-winmenu.sln`
Expected: build succeeds

Run the app; right-click the tray icon. The menu must be dark with accent hover, the version line muted, "More Actions" dropdown dark too, "Start with Windows" check mark visible when checked, "Download Update" (if visible) in the link colour.

- [ ] **Step 8: Run the full test suite and commit**

Run: `~/.dotnet-local/dotnet test pass-winmenu.sln`
Expected: all PASS

```bash
git add pass-winmenu/src/Notifications/ pass-winmenu-tests/Notifications/ThemedMenuColoursTests.cs
git commit -m "Theme the tray menu with a renderer driven by the shared palette"
```

---

### Task 6: Keyhole icon assets and generation script

**Files:**
- Rewrite: `resources/icon.svg`, `resources/icon-plain.svg`, `resources/icon-ahead.svg`, `resources/icon-behind.svg`, `resources/icon-diverged.svg`
- Create: `resources/icon-16.svg`, `resources/generate_icons.py`
- Regenerate: `pass-winmenu/embedded/pass-winmenu-plain.ico`, `-ahead.ico`, `-behind.ico`, `-diverged.ico`
- Modify: `.gitignore` (add `resources/preview/`)

**Interfaces:**
- Consumes: nothing from other tasks (fully independent)
- Produces: the four `.ico` files consumed by `EmbeddedResources.cs` and `<ApplicationIcon>` (names unchanged, so no code changes)

- [ ] **Step 1: Rewrite the master SVG**

`resources/icon.svg` (design source of truth; overlays toggled by changing `display`):

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <!-- pass-winmenu-2 keyhole icon. The .ico files are generated by generate_icons.py,
       which mirrors this geometry (plus a faint dark outline for light taskbars). -->
  <g id="icon">
    <path fill="#ffffff" fill-rule="evenodd"
          d="M16 3 a13 13 0 1 0 0 26 a13 13 0 1 0 0 -26 z
             M16 7.3 A4.2 4.2 0 0 0 13.4 14.8 L11.2 23.5 L20.8 23.5 L18.6 14.8 A4.2 4.2 0 0 0 16 7.3 Z"/>
  </g>
  <g id="overlay-ahead" display="none">
    <circle cx="24.6" cy="24.6" r="6.4" fill="#3fb950"/>
    <path fill="#ffffff" d="M24.6 21 l3.4 3.8 h-2.1 v3.6 h-2.6 v-3.6 h-2.1 z"/>
  </g>
  <g id="overlay-behind" display="none">
    <circle cx="24.6" cy="24.6" r="6.4" fill="#388bfd"/>
    <path fill="#ffffff" d="M24.6 28.2 l3.4 -3.8 h-2.1 v-3.6 h-2.6 v3.6 h-2.1 z"/>
  </g>
  <g id="overlay-diverged" display="none">
    <circle cx="24.6" cy="24.6" r="6.4" fill="#db6d28"/>
    <path fill="#ffffff" d="M24.6 21.2 l3.2 3.4 -3.2 3.4 -3.2 -3.4 z"/>
  </g>
</svg>
```

- [ ] **Step 2: Rewrite the standalone variant SVGs**

`resources/icon-plain.svg`:

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <path fill="#ffffff" fill-rule="evenodd"
        d="M16 3 a13 13 0 1 0 0 26 a13 13 0 1 0 0 -26 z
           M16 7.3 A4.2 4.2 0 0 0 13.4 14.8 L11.2 23.5 L20.8 23.5 L18.6 14.8 A4.2 4.2 0 0 0 16 7.3 Z"/>
</svg>
```

`resources/icon-ahead.svg` (the mask punches the transparent gap between glyph and badge):

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <mask id="gap">
    <rect width="32" height="32" fill="#ffffff"/>
    <circle cx="24.6" cy="24.6" r="8" fill="#000000"/>
  </mask>
  <path fill="#ffffff" fill-rule="evenodd" mask="url(#gap)"
        d="M16 3 a13 13 0 1 0 0 26 a13 13 0 1 0 0 -26 z
           M16 7.3 A4.2 4.2 0 0 0 13.4 14.8 L11.2 23.5 L20.8 23.5 L18.6 14.8 A4.2 4.2 0 0 0 16 7.3 Z"/>
  <circle cx="24.6" cy="24.6" r="6.4" fill="#3fb950"/>
  <path fill="#ffffff" d="M24.6 21 l3.4 3.8 h-2.1 v3.6 h-2.6 v-3.6 h-2.1 z"/>
</svg>
```

`resources/icon-behind.svg`: same as icon-ahead.svg but badge `fill="#388bfd"` and symbol path `d="M24.6 28.2 l3.4 -3.8 h-2.1 v-3.6 h-2.6 v3.6 h-2.1 z"`.

`resources/icon-diverged.svg`: same but badge `fill="#db6d28"` and symbol path `d="M24.6 21.2 l3.2 3.4 -3.2 3.4 -3.2 -3.4 z"`.

`resources/icon-16.svg` (pixel-hinted small-size variant, documentation of the hinted geometry):

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16">
  <path fill="#ffffff" fill-rule="evenodd"
        d="M8 1 a7 7 0 1 0 0 14 a7 7 0 1 0 0 -14 z
           M8 3.3 A2.3 2.3 0 0 0 6.7 7.4 L5.6 12 L10.4 12 L9.3 7.4 A2.3 2.3 0 0 0 8 3.3 Z"/>
</svg>
```

- [ ] **Step 3: Create generate_icons.py**

`resources/generate_icons.py`:

```python
"""Generates the pass-winmenu-2 icons (keyhole glyph + git-sync badges).

Draws with Pillow primitives, mirroring the geometry in resources/icon.svg,
supersampled 8x, and packs multi-size .ico files (PNG-compressed entries)
into pass-winmenu/embedded/. A faint dark outline is baked in so the white
glyph stays visible on light taskbars. 16/20 px entries use pixel-hinted
geometry (resources/icon-16.svg).

Usage: python generate_icons.py    (requires: pip install pillow)
"""
import io
import struct
from pathlib import Path

from PIL import Image, ImageChops, ImageDraw

SS = 8  # supersampling factor
SIZES = [16, 20, 24, 32, 48, 256]
OUT_DIR = Path(__file__).resolve().parent.parent / 'pass-winmenu' / 'embedded'
PREVIEW_DIR = Path(__file__).resolve().parent / 'preview'

WHITE = (255, 255, 255, 255)
OUTLINE = (0, 0, 0, 140)

# Base geometry, 32-unit box (resources/icon.svg)
BASE = {
    'units': 32.0,
    'disc_c': (16.0, 16.0),
    'disc_r': 13.0,
    'hole_c': (16.0, 11.5),
    'hole_r': 4.2,
    'trap': [(13.4, 14.8), (18.6, 14.8), (20.8, 23.5), (11.2, 23.5)],
    'outline': 0.8,
}
# Pixel-hinted geometry for 16/20 px, 16-unit box (resources/icon-16.svg)
HINTED = {
    'units': 16.0,
    'disc_c': (8.0, 8.0),
    'disc_r': 7.0,
    'hole_c': (8.0, 5.6),
    'hole_r': 2.3,
    'trap': [(6.7, 7.4), (9.3, 7.4), (10.4, 12.0), (5.6, 12.0)],
    'outline': 0.5,
}

# Badges, 32-unit box
BADGE_UNITS = 32.0
BADGE_C = (24.6, 24.6)
BADGE_R = 6.4
BADGE_GAP = 1.6
BADGES = {
    'ahead': ((63, 185, 80, 255),
              [(24.6, 21.0), (28.0, 24.8), (25.9, 24.8), (25.9, 28.4),
               (23.3, 28.4), (23.3, 24.8), (21.2, 24.8)]),
    'behind': ((56, 139, 253, 255),
               [(24.6, 28.2), (28.0, 24.4), (25.9, 24.4), (25.9, 20.8),
                (23.3, 20.8), (23.3, 24.4), (21.2, 24.4)]),
    'diverged': ((219, 109, 40, 255),
                 [(24.6, 21.2), (27.8, 24.6), (24.6, 28.0), (21.4, 24.6)]),
}


def circle_box(cx, cy, r, scale):
    return [(cx - r) * scale, (cy - r) * scale, (cx + r) * scale, (cy + r) * scale]


def inset_polygon(points, amount):
    """Moves each vertex towards the centroid so edges shift inwards by roughly
    `amount` units. Exact edge offsetting is unnecessary at these sizes."""
    n = len(points)
    cx = sum(p[0] for p in points) / n
    cy = sum(p[1] for p in points) / n
    result = []
    for x, y in points:
        dx, dy = x - cx, y - cy
        dist = (dx * dx + dy * dy) ** 0.5
        factor = max(dist - amount, 0.0) / dist
        result.append((cx + dx * factor, cy + dy * factor))
    return result


def keyhole_mask(canvas, g, scale, grow):
    """Alpha mask of the disc with the keyhole punched out. A positive `grow`
    expands the disc and shrinks the cutout, producing the outline silhouette."""
    mask = Image.new('L', (canvas, canvas), 0)
    draw = ImageDraw.Draw(mask)
    draw.ellipse(circle_box(*g['disc_c'], g['disc_r'] + grow, scale), fill=255)
    cut = Image.new('L', (canvas, canvas), 0)
    cut_draw = ImageDraw.Draw(cut)
    cut_draw.ellipse(circle_box(*g['hole_c'], g['hole_r'] - grow, scale), fill=255)
    cut_draw.polygon([(x * scale, y * scale) for x, y in inset_polygon(g['trap'], grow)], fill=255)
    return ImageChops.subtract(mask, cut)


def draw_glyph(size, badge=None):
    g = HINTED if size <= 20 else BASE
    scale = size * SS / g['units']
    canvas = size * SS
    img = Image.new('RGBA', (canvas, canvas), (0, 0, 0, 0))

    img.paste(Image.new('RGBA', (canvas, canvas), OUTLINE),
              mask=keyhole_mask(canvas, g, scale, g['outline']))
    img.paste(Image.new('RGBA', (canvas, canvas), WHITE),
              mask=keyhole_mask(canvas, g, scale, 0.0))

    if badge is not None:
        bscale = size * SS / BADGE_UNITS
        colour, symbol = BADGES[badge]
        alpha = img.getchannel('A')
        punch = Image.new('L', (canvas, canvas), 0)
        ImageDraw.Draw(punch).ellipse(circle_box(*BADGE_C, BADGE_R + BADGE_GAP, bscale), fill=255)
        img.putalpha(ImageChops.subtract(alpha, punch))
        draw = ImageDraw.Draw(img)
        draw.ellipse(circle_box(*BADGE_C, BADGE_R, bscale), fill=colour)
        draw.polygon([(x * bscale, y * bscale) for x, y in symbol], fill=WHITE)

    return img.resize((size, size), Image.LANCZOS)


def write_ico(path, images):
    header = struct.pack('<HHH', 0, 1, len(images))
    entries = []
    blobs = []
    offset = 6 + 16 * len(images)
    for img in images:
        buf = io.BytesIO()
        img.save(buf, 'PNG')
        blob = buf.getvalue()
        dim = img.width % 256  # 256 px is stored as 0
        entries.append(struct.pack('<BBBBHHII', dim, dim, 0, 0, 1, 32, len(blob), offset))
        blobs.append(blob)
        offset += len(blob)
    path.write_bytes(header + b''.join(entries) + b''.join(blobs))


def main():
    PREVIEW_DIR.mkdir(exist_ok=True)
    for variant, badge in [('plain', None), ('ahead', 'ahead'),
                           ('behind', 'behind'), ('diverged', 'diverged')]:
        images = [draw_glyph(size, badge) for size in SIZES]
        write_ico(OUT_DIR / f'pass-winmenu-{variant}.ico', images)
        for img in images:
            if img.width in (16, 32, 48):
                img.save(PREVIEW_DIR / f'{variant}-{img.width}.png')
        print(f'wrote pass-winmenu-{variant}.ico')


if __name__ == '__main__':
    main()
```

- [ ] **Step 4: Add preview dir to .gitignore**

Append a line `resources/preview/` to the repository `.gitignore`. If no `.gitignore` exists at the repo root, create one containing only that line.

- [ ] **Step 5: Generate and inspect**

```powershell
python -m pip install pillow
python resources/generate_icons.py
```

Expected output: four `wrote pass-winmenu-*.ico` lines.

Inspect `resources/preview/plain-16.png`, `plain-32.png`, `plain-48.png`, `ahead-32.png`, `behind-32.png`, `diverged-32.png` (Read them as images): keyhole must be crisp and centred, badges separated from the glyph by a transparent gap, no clipping at edges. Iterate on geometry constants if 16 px looks muddy.

Verify ico structure:

```powershell
python -c "import struct; d=open('pass-winmenu/embedded/pass-winmenu-plain.ico','rb').read(); n=struct.unpack('<H', d[4:6])[0]; print(n, [struct.unpack('<B', d[6+16*i:7+16*i])[0] or 256 for i in range(n)])"
```

Expected: `6 [16, 20, 24, 32, 48, 256]`

- [ ] **Step 6: Verify in the app**

Run: `~/.dotnet-local/dotnet build pass-winmenu.sln`
Run the app: the tray icon must show the new keyhole. Check Explorer shows the new icon on `pass-winmenu.exe` (the `.ico` is compiled in via `<ApplicationIcon>`).

- [ ] **Step 7: Commit**

```bash
git add resources/ pass-winmenu/embedded/*.ico .gitignore
git commit -m "Replace app icon with keyhole glyph, add Pillow-based ico generator"
```

---

### Task 7: End-to-end verification

**Files:** none (verification only)

- [ ] **Step 1: Clean build and full test suite**

```powershell
& "$env:USERPROFILE\.dotnet-local\dotnet" build pass-winmenu.sln -c Debug
& "$env:USERPROFILE\.dotnet-local\dotnet" test pass-winmenu.sln
```

Expected: build succeeds, all tests pass

- [ ] **Step 2: Walk every UI surface**

Run `pass-winmenu/bin/Debug/net8.0-windows10.0.19041.0/pass-winmenu.exe` and check each surface (screenshot each, e.g. with a PowerShell CopyFromScreen snippet or manually, and inspect):

1. Tray icon: new keyhole glyph, correct at the current DPI; if feasible, also switch Windows display scaling to 150% and re-check the tray icon crispness (spec requires 100% and 150%)
2. Tray right-click menu: dark, accent hover, dark More Actions dropdown, muted version header, themed check mark on "Start with Windows"
3. About: keyhole mark, dark background, dark title bar, accent links, themed Close button
4. Add new Password (PasswordWindow): themed text boxes, Generate button, slider, checkbox
5. Edit Password File (EditWindow): themed group box, accent divider, buttons
6. Windows Hello setup (PassphraseWindow): themed PasswordBox and buttons
7. View Log (LogViewer): dark text box, themed scrollbar
8. Password selector + password details: unchanged appearance (regression check)
9. Balloon notification: still appears (unchanged)

- [ ] **Step 3: Sync badge check**

Temporarily verify badge icons render: in a git-enabled password store, create a local commit (ahead state) or use `git` to diverge, then confirm the tray icon shows the badge. Alternatively confirm visually from `resources/preview/*.png`.

- [ ] **Step 4: Fix anything found, re-run, commit fixes**

Any visual defect: fix in the task's file, rebuild, re-check, then commit with a descriptive message.
