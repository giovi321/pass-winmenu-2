<Project>
  <PropertyGroup>
    <!-- LibGit2Sharp >= 0.29 ships its native binaries under modern runtime
         identifiers (win-x64), so the UseRidGraph workaround previously required
         for 0.26.2 (legacy win7-x64 RID) is no longer needed. -->
  </PropertyGroup>
</Project>
